---
description: Automatically deploy changes to GitHub
---

// turbo-all
1. Stage all changes
2. Commit changes with a descriptive message
3. Push changes to the main branch

```bash
git add .
git commit -m "chore: auto-deploy changes"
git push origin main
```
